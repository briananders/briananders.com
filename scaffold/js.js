/**
 * Generates initial JavaScript client script boilerplate for a newly scaffolded page.
 *
 * @returns {string} Generated JavaScript template string.
 */
module.exports = () => `const ready = require('../_modules/document-ready');

ready.document(() => {

});
`;

