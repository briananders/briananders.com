<nav class="nav main" aria-label="Main navigation">
  <div class="content">
    <%- link(locals.name, { class: "brand", href: "/", title: locals.name, }) %>
    <button id="activate-menu" class="menu link" aria-expanded="false" aria-controls="mobile-nav-tray">
      Menu
    </button>
    <div class="mobile-nav-tray" id="mobile-nav-tray" aria-hidden="true" role="menu" style="display: none;">
      <%- link('Home', { href: '/', role: 'menuitem', }) %>
      <%
        let basePages = getChildPages('');
        basePages.forEach(({ url, data }) => {
      %>
        <%- link(page.data.title, { href: `/${page.url}`, role: 'menuitem', }) %>
      <% } %>
    </div>
  </nav>
</div>
